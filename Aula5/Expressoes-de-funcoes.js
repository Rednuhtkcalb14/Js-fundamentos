// Funções declaradas:

function declarada() {
    // código da função
}

// Expressões de função

const expressão = function(teste) {return teste}

// Diferenças:

// Funções declaradas são içadas "hoisting" para o "topo" do código, podem
// ser usadas mesmo antes de terem sido escritas

// Expressões de funções não podem ser usadas antes de serem declaradas,
// são usadas mais para funções menores, não possuem nome